shell waits for more input when we only type \\
heap buffer overflow when erasing a character (try with -fsanitize=address)
first token of split line is empty when command is a parenthesis

multiline quotes isn't working
try: echo "
hello
world
"

single quotes multiline prompt is dquote>, it should be quote>
unfinished \ multiline prompt is also dquote>, bash --posix only prints >

CTRL-C can't delete the current line, i'll need a function that erases the current line to handle that
